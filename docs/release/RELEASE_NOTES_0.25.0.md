# Mosaic Tokenizer 0.25.0

Supported Python binding release.

- deterministic `mosaic-tokenizer` pure-Python wheel;
- ownership-safe context managers over the stable C ABI;
- exact encode/decode/token spans and integrated derived views;
- TokenDocument persistence and bounded parallel batch execution;
- runtime policy/metrics and privacy-preserving observer support;
- no duplicate Python tokenizer implementation.
