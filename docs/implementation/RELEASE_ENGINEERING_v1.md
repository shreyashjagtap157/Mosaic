# Release Engineering v1

Mosaic distributions are deterministic tar.gz archives with normalized timestamps/ownership. A release contains SHA-256 checksums, a release manifest, an SPDX 2.3 JSON SBOM, and an in-toto/SLSA-shaped provenance statement. The provenance records the source checksum-manifest identity and every staged artifact digest.

Cross-platform core qualification deliberately disables optional OpenSSL/ICU-dependent features so the tokenizer core proves it has no hidden dependency on them. Full Linux qualification enables all available optional trust and Unicode differential tests.

Canonical pack identity remains content-addressed and independent of the release bundle. Release provenance authenticates what was built; pack trust authenticates publisher/content relationships. These are separate security layers.

Product release artifacts use the canonical four-part `S.M.N.P` version from the repository `VERSION` file. Release qualification runs `tools/set_version.py --check` and the cross-platform source checksum validator before packaging so CMake metadata, headers, CLI tools, Python package metadata, manifests, SBOM/provenance, and archive names cannot drift independently. The consolidated local entrypoint is `tools/validate_release_readiness.py`, which chains the repository, Miri, fuzz-smoke, space-grade docs, and release-package checks, and the release-qualification workflow now calls the same entrypoint for the Linux package gate. A faster local convenience path may skip Miri via `--skip-miri` or `make release-readiness-fast` when the long-running interpreter gate is intentionally deferred. `docs/implementation/EXTERNAL_QUALIFICATION.md` is the canonical checklist for the macOS, cargo-fuzz, race-detector, and architecture-fleet gates that still require external runners. `tools/validate_release_matrix.py` keeps the Makefile, workflow, README, and release-engineering doc aligned, while `tools/validate_open_gates.py` keeps the remaining 1.0.0 blocker summary synchronized across the candidate documentation. Pack registry SemVer and frozen ABI/format versions remain separate contracts.
The constrained-desktop evidence path is separately maintained through `tools/run_low_memory_profile.py`, the `make low-memory-profile` convenience target, and the bounded manifest example under `benches/low_memory_4gb.toml`.
The companion host-metadata capture path is `tools/record_machine_profile.py` or `make low-memory-machine-profile`; use it to record the benchmark machine record fields called out in `docs/implementation/REFERENCE_HARDWARE.md`.
The one-step bundle capture path is `tools/run_low_memory_evidence.py` or `make low-memory-evidence`; use it when you want the host profile and benchmark record produced together under `benches/low_memory_4gb.runs/`.
Windows desktop packaging is maintained by `tools/package_windows_app.ps1`, which stages the install tree, cleans stale staged files, and emits the Inno Setup installer `dist/windows/MosaicCompressorSetup-<version>-x64.exe`.
